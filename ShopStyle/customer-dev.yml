spring:
  datasource:
    username: postgres
    password: compasso
    url: jdbc:postgresql://localhost:5432/customertest
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
