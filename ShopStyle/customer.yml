spring:
  datasource:
    username: postgres
    password: compasso
    url: jdbc:postgresql://localhost:5432/customer
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: create-drop
