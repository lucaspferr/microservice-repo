spring:
  datasource:
    username: postgres
    password: compasso
    url: jdbc:postgresql://localhost:5432/customerteste1
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: create-drop
